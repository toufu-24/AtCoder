#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N;
    cin >> N;
    int P, Q, R;
    cin >> P >> Q >> R;
    vector<int> A(N);
    for (int i = 0; i < N; i++)
    {
        cin >> A[i];
    }
    int x, y, z, w;
}