#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;
    vector<int> A(T);
    vector<int> B(T);
    for (int i = 0; i < T; i++)
    {
        cin >> A[i] >> B[i];
    }
    for (int i = 0; i < T; i++)
    {
        int Y_kX = A[i] - B[i];
    }
}