{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LLDB: Launch (g++-14, integrated terminal)",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "terminal": "integrated",
      "preLaunchTask": "build (g++-14 current file)"
    }
  ]
}
