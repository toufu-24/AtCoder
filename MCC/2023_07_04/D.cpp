#include <bits/stdc++.h>
using namespace std;

int main()
{
    while (1)
    {
        int64_t S, R, A, E;
        string X, Y;
        cin >> X;
        if (X == "#")
        {
            break;
        }
        cin >> Y;
        cin >> A >> E >> S >> R;
    }
}